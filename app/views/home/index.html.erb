<div class="message_form">
  <%= form_for(Message.new) do |f| %>
    <div class="field">
      <%= f.text_area :body, :rows => 3, :cols => 60 %>
    </div>
    <div class="actions">
      <%= f.submit "投稿"%>
    </div>
  <% end %>
</div>

<%= render(Message.order("created_at DESC")) %>
