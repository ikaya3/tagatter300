<%= form_for(Message.new) do |f| %>
  <div class="field">
    <%= f.text_area :body, :rows => 3, :cols => 40 %>
  </div>
  <div class="actions">
    <%= f.submit "投稿"%>
  </div>
<% end %>

<table>
<%= render(Message.order("created_at DESC")) %>
</table>

